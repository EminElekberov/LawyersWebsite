
@model IList<PracticeAbout>

@if (TempData["Success"] != null)
{
    <div class="alert alert-primary" role="alert">
        @TempData["Success"]
    </div>
}

<div>
    <a href="/Admin/PracticeAbout/Create" class="btn btn-success" style="width:100%">Add new slider</a>
</div>

<table class="table table-bordered">
    <thead>
        <tr>
            <th scope="col">ID</th>
            <th scope="col">Icon</th>
            <th scope="col">Header</th>
            <th scope="col">Description</th>
            <th scope="col">Learn Text</th>
            <th scope="col">Learn Text Link</th>
            <th>Edit/Delete</th>
        </tr>
    </thead>

    <tbody>

        @foreach (var item in Model)
        {
            <tr>
                <th scope="row">@item.Id</th>
                <td>@Html.Raw(item.IconLink)</td>
                <td>@item.Header</td>
                <td>@item.Description</td>
                <td>@item.Learntext</td>
                <td>@item.LearnLink</td>

                <td>
                    <a href="/Admin/PracticeAbout/Edit/@item.Id" style="color:red"><i class="fas fa-edit"></i></a>
                    <a href="/Admin/PracticeAbout/Delete/@item.Id" onclick="return confirm('Silmek isdediyinizden eminsinizmi?')" style="color:red"><i class="fas fa-trash-alt"></i></a>
                    <br />
                    <a href="/Admin/PracticeAbout/Details/@item.Id" style="color:red">Etrafli</a>
                </td>
            </tr>

        }
    </tbody>
</table>